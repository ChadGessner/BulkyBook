@model IEnumerable<Company>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Company List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Company" asp-action="Upsert" class="btn btn-primary">
                <i class=" bi bi-plus-circle"></i> Create Company 
            </a>
        </div>
    </div>
</div>
<br />
<br />
<h1>Index Companies</h1>

<table id="tblData" class="table table-bordered table-striped" style="width: 100%">
    <thead>
        <tr>
            <th>
                Company Name
            </th>
            <th>
                Company Street Address
            </th>
            <th>
                Company City
                
            </th>
            <th>
                Company State
                
            </th>
            <th>
                Company Postal Code
                
            </th>
            <th>
                Company Phone Number
                
            </th>
            <th></th>
        </tr>
    </thead>
    @*<tbody>
        @foreach (var company in Model)
        {
            <tr>
                <td width="50%">
                    @company.Name
                </td>
                <td width="30%">
                    @company.PhoneNumber
                </td>
                <td width="50%">
                    @company.StreetAddress
                </td>
                <td width="30%">
                    @company.City
                </td>
                <td width="50%">
                    @company.State
                </td>
                <td width="30%">
                    @company.PostalCode
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a class="btn btn-primary mx-2" asp-controller="Company" asp-action="Edit" asp-route-id="@company.Id"><i class="bi bi-pencil-square"></i>Edit</a>
                    </div>
                    <div class="w-75 btn-group" role="group">
                        <a class="btn btn-danger mx-2" asp-controller="Company" asp-action="Delete" asp-route-id="@company.Id"><i class="bi bi-fire"></i>Delete</a>
                    </div>
                </td>

            </tr>
        }
    </tbody>*@
</table>
@section Scripts{
    <script src="~/js/company.js">
    </script>
}